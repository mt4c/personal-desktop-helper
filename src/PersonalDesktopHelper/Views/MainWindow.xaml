<Window x:Class="PersonalDesktopHelper.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Personal Desktop Helper" Height="650" Width="900" MinHeight="450" MinWidth="650">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="New chat" Padding="12,5" Margin="0,0,8,0"
                        IsEnabled="{Binding CanConfigure}" Click="OnNewChatClick" />
                <Button Content="Copilot options" Padding="12,5" Click="OnOptionsClick" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="GitHub Copilot" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Status}" Margin="0,4,12,0" Foreground="DimGray" TextWrapping="Wrap" />
            </StackPanel>
        </DockPanel>
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Firebrick"
                   TextWrapping="Wrap" Margin="0,0,0,8" />
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Background="White">
            <ScrollViewer x:Name="TranscriptScroll" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Messages}" Margin="12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F3F5F7" CornerRadius="4" Padding="12" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role}" FontWeight="SemiBold" Margin="0,0,0,6" />
                                    <TextBox Text="{Binding Content, Mode=OneWay}" IsReadOnly="True"
                                             TextWrapping="Wrap" BorderThickness="0" Padding="0"
                                             Background="Transparent" IsReadOnlyCaretVisible="True" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <TextBox x:Name="PromptTextBox" Grid.Row="3" Margin="0,12,0,8" Padding="10"
                 Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}"
                 MinHeight="75" MaxHeight="180" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" PreviewKeyDown="OnPromptKeyDown"
                 IsEnabled="{Binding CanConfigure}" />
        <DockPanel Grid.Row="4">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Stop" Padding="16,6" Margin="0,0,8,0"
                        IsEnabled="{Binding IsBusy}" Click="OnStopClick" />
                <Button Content="Send" Padding="16,6"
                        IsEnabled="{Binding CanSend}" Click="OnSendClick" />
            </StackPanel>
            <TextBlock Text="Enter to send; Shift+Enter for a new line. Chat stays in memory until Quit."
                       Foreground="DimGray" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,12,0" />
        </DockPanel>
    </Grid>
</Window>
